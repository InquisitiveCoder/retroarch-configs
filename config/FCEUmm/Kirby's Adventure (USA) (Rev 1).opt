# These settings center the image relative to the HUD at the bottom,
# and don't cut off any part of the HUD or pause screen.
fceumm_overscan_h_left = "12"
fceumm_overscan_h_right = "4"

# Options are always stand-alone files, so game and content directory
# overrides should include the core options file.
#include "FCEUmm.opt"
